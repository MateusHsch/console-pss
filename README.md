# Process-scheduler-simulator

A process scheduler simulator thats receive a group of parameters and a .csv file with process, scheduling them.

## Compile Program

`gcc src/*.c -o main`

## Run Program

`main FCFS 100 200 300 400 samples/10_processes.csv`  
`main FCFS 1 2 3 4 samples/10_processes.csv`  
